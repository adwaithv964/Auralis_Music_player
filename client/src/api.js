/**
 * Legacy shim — re-exports from the new services/api location.
 * All new code should import from './services/api' directly.
 * This file exists purely for backward compatibility.
 */
export { api } from './services/api';
